# project outputs (videos, frames, audio) — never committed
projects/
# python
__pycache__/
*.pyc
.venv/
venv/
# local media library (music/SFX/brand kits — licensed for use, not redistribution)
library/
# local env / tooling
.DS_Store
*.log
node_modules/
# intel caches (Phase 1)
.cache/
