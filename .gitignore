# operator content — lives in the private `explainer-content` repo, symlinked in here
/projects
/channel
/research
# python
__pycache__/
*.pyc
.venv/
venv/
# local media library (music/SFX/brand kits — licensed for use, not redistribution)
library/
# operator photos (personal, never committed)
dave_selfies/
coffee_mugs/
# local env / tooling
.DS_Store
*.log
node_modules/
# intel caches (Phase 1)
.cache/
# operator/brand assets (personal media, not redistributed)
book_cover/
# SECRETS — OAuth client secrets / tokens must never be committed (this repo is public).
# Real home is ~/.config/explainer2/. These patterns catch anything dropped in-repo by mistake.
auth/
*client_secret*
token_*.json
